<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Details</title>
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body>
    <div class="main w-full min-h-screen bg-zinc-900 p-10 text-white flex">
      <img src="<%=book.imageLink %>" alt="" class="w-full" />
      <div class="pl-20">
        <h1 class="pt-7 text-4xl font-bold"><%= book.title %></h1>
        <p class="font-semibold text-2xl pt-8"><%= book.author %></p>
        <p class="text-xl text-slate-400 font-semibold pt-24">
          <%= book.about %>
        </p>
        <div class="flex pt-24">
          <p class="text-3xl text-orange-600 font-bold"><%= book.price %></p>

          <form action="/books/addToCart/<%= book.id %>" method="POST">
            <button
              type="submit"
              class="bg-blue-500 text-white w-28 p-2 ml-10 rounded hover:bg-blue-700"
            >
              Add to Cart
            </button>
          </form>
        </div>
      </div>
      <a href="/books/homepage">
        <button class="bg-blue-800 w-24 p-3 rounded-md ml-28 hover:bg-blue-500">
          back
        </button></a
      >
    </div>
  </body>
</html>
